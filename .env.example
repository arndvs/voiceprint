# Copy to .env and fill in. NEVER commit .env.
# Optional — only needed when using --diarize on the transcriber.
HF_TOKEN=hf_YOUR_TOKEN_HERE

# Optional — override the Whisper model used by scripts/transcribe.py
# TRANSCRIBE_MODEL=turbo
